{
  "name": "ak-realestate",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "bun serve.ts",
    "build": "rm -rf dist && mkdir -p dist && cp -r public/. dist/ && cp _routes.json dist/ 2>/dev/null || true",
    "build:dev": "rm -rf dist && mkdir -p dist && cp -r public/. dist/ && cp _routes.json dist/ 2>/dev/null || true",
    "preview": "bun serve.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.0"
  }
}
