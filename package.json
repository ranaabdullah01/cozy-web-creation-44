{
  "name": "ak-realestate",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "bun serve.ts",
    "build": "echo 'No build step — Cloudflare Pages Functions'",
    "build:dev": "echo 'No build step — Cloudflare Pages Functions'",
    "preview": "bun serve.ts"
  }
}
