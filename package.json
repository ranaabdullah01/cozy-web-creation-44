{
  "name": "ak-realestate",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "bun serve.ts",
    "build": "echo 'Static site — nothing to build'",
    "preview": "bun serve.ts"
  }
}
